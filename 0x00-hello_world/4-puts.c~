#include <stdio.h>
/*
 * main - entrypoint
 *
 * Decription: 'This program is a practice of the use of puts instead of putchar'
 *
 * return: Always 0 (success)
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle");

return (0);
}
