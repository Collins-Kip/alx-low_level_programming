#include<stdio.h>
#include"main.h"

/**
 * main - checks for uppercase characters
 *
 * return 1;
 * always when the character is uppercase
 * 0 if not
 */

int _isupper(int c);
{
int c;
char l;
printf ('Enter the letter \n');
scanf ('%c', l);
if (l >= 48, l <= 57)
{
return (1);
}
else
{
return (0);
}
}
